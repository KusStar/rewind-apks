name: CheckURL

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: URL Checker
      uses: paramt/url-checker@master
      with:
          files: "README.md"
